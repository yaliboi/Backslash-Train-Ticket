// import { AllNodes } from "../../types/graph/allNodes";
// import { RawGraphData } from "../../types/graph/rawGraphData";
// import { TreeNode } from "../../types/graph/treeView";

// export const rawToTreeView = (raw: RawGraphData): TreeNode[] => {
//     const treeView = []
    
//     // tree roots will be nodes who have no parents
//     const treeRoots = raw.nodes.filter(node => !raw.edges.find(edge => edge.to.includes(node.name)))

//     treeView = treeRoots.map(node => {

//     })

// }